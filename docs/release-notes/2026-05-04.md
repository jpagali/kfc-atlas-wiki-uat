---
title: Release 2026.05.04
---

<div className="release-notes-page">
  <div className="release-detail-hero">
    <div className="release-card-topline">
      <span className="release-card-date">May 4, 2026</span>
      <span className="release-status release-status--live">Live</span>
    </div>
    <h1>Release 2026.05.04</h1>
    <p className="release-notes-lead">
      This release promotes the current KFC Australia prototype into Atlas Wiki, replaces the older hosted sneak-peek
      build cleanly, and adds better review controls so stakeholders can restart the journey or switch into a
      prototype-only fullscreen mode without losing context.
    </p>

    <div className="release-summary-grid">
      <div className="release-summary-card">
        <span className="release-summary-label">Prototype</span>
        <strong>The hosted Atlas Peek experience now matches the current local KFC AU prototype instead of an older partial build.</strong>
      </div>
      <div className="release-summary-card">
        <span className="release-summary-label">Data</span>
        <strong>The AU menu-driven prototype now runs with the promoted runtime data file and required hosted font assets.</strong>
      </div>
      <div className="release-summary-card">
        <span className="release-summary-label">Review UX</span>
        <strong>Readers can now restart the journey and open a prototype-only fullscreen review mode without clutter inside the phone frame.</strong>
      </div>
      <div className="release-summary-card">
        <span className="release-summary-label">Cleanup</span>
        <strong>Legacy hosted prototype data was removed so the wiki no longer mixes old and new sneak-peek logic.</strong>
      </div>
      <div className="release-summary-card">
        <span className="release-summary-label">Japan sunset</span>
        <strong>The retired Japan locale and its JP-specific assets were removed so Atlas Wiki now runs as a leaner single-locale site.</strong>
      </div>
    </div>
  </div>

  <section className="release-section">
    <div className="release-section-heading">
      <span className="release-section-label">Key highlights</span>
      <h2>What changed most</h2>
    </div>

    <div className="release-card-grid">
      <article className="release-card">
        <h3>The hosted prototype now reflects the real current AU state</h3>
        <p>
          The old hosted sneak-peek experience was replaced with the current local KFC Australia prototype HTML rather
          than patched forward incrementally, so the wiki now shows the same cart, checkout WIP treatment, and
          menu-driven ordering behavior that exists in the active local working version.
        </p>
        <div className="release-pill-row">
          <span className="release-pill">Clean promotion</span>
          <span className="release-pill">KFC AU prototype</span>
        </div>
      </article>

      <article className="release-card">
        <h3>Supporting runtime assets were promoted with it</h3>
        <p>
          The AU menu data file and the Kentucky Fried font assets required by the prototype are now hosted in the wiki
          repo, which removes the gap between local-only dependencies and the published GitHub Pages version.
        </p>
        <div className="release-pill-row">
          <span className="release-pill">Menu data</span>
          <span className="release-pill">Font hosting</span>
        </div>
      </article>

      <article className="release-card">
        <h3>Prototype review is easier to control</h3>
        <p>
          The sneak-peek page now includes a restart action outside the phone shell and a prototype-only fullscreen
          review mode, so readers can recover from deep checkout states and focus on the app flow without the rest of
          the wiki getting in the way.
        </p>
        <div className="release-pill-row">
          <span className="release-pill">Restart journey</span>
          <span className="release-pill">Fullscreen review</span>
        </div>
      </article>

      <article className="release-card">
        <h3>The route now behaves consistently in dev and hosted builds</h3>
        <p>
          The sneak-peek wrapper now points at the real hosted prototype asset path and uses a message-based reset hook,
          which avoids the earlier mismatch where the iframe could render differently between local development and the
          built site.
        </p>
        <div className="release-pill-row">
          <span className="release-pill">Pathing fix</span>
          <span className="release-pill">Embed stability</span>
        </div>
      </article>

      <article className="release-card">
        <h3>The retired Japan locale was removed cleanly</h3>
        <p>
          The old <code>ja-JP</code> locale tree, JP search index output, locale-specific build wiring, and Japan-only
          hosted assets were removed so the wiki now reflects the active programme footprint instead of carrying
          inactive locale overhead.
        </p>
        <div className="release-pill-row">
          <span className="release-pill">Locale retirement</span>
          <span className="release-pill">Build simplification</span>
        </div>
      </article>
    </div>
  </section>

  <section className="release-section">
    <div className="release-section-heading">
      <span className="release-section-label">Release themes</span>
      <h2>What this release was really about</h2>
    </div>

    <div className="release-theme-stack">
      <article className="release-theme">
        <h3>Promotion over hybridization</h3>
        <p>
          The main goal in this window was not to preserve every older hosted prototype affordance. It was to promote
          the actual current AU prototype state into the wiki cleanly so the review surface is trustworthy again.
        </p>
      </article>

      <article className="release-theme">
        <h3>Review loops should not dead-end at checkout</h3>
        <p>
          Readers often arrive at later prototype states and then need a clean way back to the beginning. Restart and
          fullscreen controls now make that review loop much more usable for walkthroughs and stakeholder demos.
        </p>
      </article>

      <article className="release-theme">
        <h3>Hosted prototypes need their real dependencies</h3>
        <p>
          This release closes the gap between local experimentation and the published wiki by promoting the data and
          font files the AU prototype actually depends on at runtime.
        </p>
      </article>

      <article className="release-theme">
        <h3>Retired market content should leave the stack</h3>
        <p>
          Japan has exited the programme, so the wiki should no longer build, index, or ship a second locale that no
          longer serves an active market audience.
        </p>
      </article>
    </div>
  </section>

  <section className="release-section">
    <div className="release-section-heading">
      <span className="release-section-label">Full change log</span>
      <h2>Lower-level detail</h2>
    </div>

    <details className="release-details" open>
      <summary>Atlas Peek prototype promotion</summary>
      <ul>
        <li>Replaced the hosted sneak-peek HTML with the current local KFC AU prototype implementation.</li>
        <li>Updated the sneak-peek route wrapper so the iframe uses the correct hosted prototype asset path.</li>
        <li>Kept the route as a review surface while removing stale wrapper behavior that depended on the older hosted build.</li>
      </ul>
    </details>

    <details className="release-details">
      <summary>Runtime dependencies</summary>
      <ul>
        <li>Added the promoted <code>kfc-au-menu-data.js</code> runtime file to the hosted wiki.</li>
        <li>Added the Kentucky Fried Sans, Serif, and Stripes WOFF/WOFF2 assets required by the prototype.</li>
        <li>Removed the older hosted-only prototype data file so the AU flow no longer mixes incompatible runtime sources.</li>
      </ul>
    </details>

    <details className="release-details">
      <summary>Prototype review controls</summary>
      <ul>
        <li>Added an external <code>Restart Journey</code> control that resets saved prototype state through a message-based hook.</li>
        <li>Added a prototype-only fullscreen mode for <code>/sneak-peek</code> so readers can focus on the app flow without surrounding wiki chrome.</li>
        <li>Moved restart control outside the phone frame so the prototype UI stays visually clean during demos and reviews.</li>
      </ul>
    </details>

    <details className="release-details">
      <summary>Search and build outputs</summary>
      <ul>
        <li>Refreshed the English search index as part of the release build.</li>
        <li>Verified the updated prototype route through local build output before promotion to GitHub Pages.</li>
      </ul>
    </details>

    <details className="release-details">
      <summary>Japan locale removal</summary>
      <ul>
        <li>Removed the <code>ja-JP</code> locale tree and JP-specific hosted assets that were no longer needed.</li>
        <li>Removed locale-specific app copy, search wiring, and regression checks tied to the retired Japan experience.</li>
        <li>Cut roughly <code>83.2 MB</code> of known JP-specific repo and static footprint from the wiki.</li>
      </ul>
    </details>
  </section>

  <div className="release-note-callout">
    <strong>In plain English</strong>
    <p>
      This was a trust-the-demo and trim-the-dead-weight release. Atlas Peek now reflects the real current AU prototype,
      it is easier to review without getting stuck at checkout, and the wiki no longer carries a retired Japan locale.
    </p>
  </div>
</div>
